__pycache__/
*.py[cod]

# Local credential and provider config — never commit these.
# Run 'trileaf config' to create/edit .env.
.env
.env.*
.leafhub
.claude/

# Virtual environment
.venv/

# Build / packaging artefacts
*.egg-info/
dist/
build/
*.whl

# LeafHub runtime artefacts (written by setup.sh / trileaf setup — not for distribution)
# leafhub_dist/  — integration module: probe.py + register.sh (stdlib-only fallback)
# leafhub/       — legacy path (v1)
# leafhub_probe.py — legacy single-file probe (v1)
leafhub_dist/
leafhub/
leafhub_probe.py

# Reference / local-only directories (not for distribution)
openclaw/

.cache/
models/**/.cache/
models/*
!models/.gitkeep

.DS_Store
