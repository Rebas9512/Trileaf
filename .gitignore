__pycache__/
*.py[cod]

# Local credential and provider config — never commit these.
# Run 'trileaf config' to create/edit .env.
.env
.env.*
.leafhub
.claude/

# Virtual environment
.venv/

# Build / packaging artefacts
*.egg-info/
dist/
build/
*.whl

# LeafHub legacy artefacts (v1 layout, no longer used)
leafhub/
leafhub_probe.py

# Reference / local-only directories (not for distribution)
openclaw/

.cache/
models/**/.cache/
models/*
!models/.gitkeep

.DS_Store
