__pycache__/
*.py[cod]

# Local credential and provider config — never commit these.
.env
.env.*
.leafhub
.claude/

# Virtual environment
.venv/

# Build / packaging artefacts
*.egg-info/
dist/
build/
*.whl

# LeafHub legacy artefacts
leafhub/
leafhub_probe.py

# Reference / local-only directories (not for distribution)
openclaw/

# Models (downloaded at setup, ~0.9 GB)
.cache/
models/**/.cache/
models/*
!models/.gitkeep

# Eval / test artifacts
eval_results.json
*.pytest_cache/
.pytest_cache/

# OS
.DS_Store
Thumbs.db
