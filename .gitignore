__pycache__/
*.py[cod]

# Local credential and provider config — never commit these.
# Run 'trileaf config' to create/edit .env.
.env
.env.*
.leafhub
leafhub_probe.py
.claude/

# Virtual environment
.venv/

# Build / packaging artefacts
*.egg-info/
dist/
build/
*.whl

# LeafHub local directory (probe copy or local dev install)
leafhub/

# Reference / local-only directories (not for distribution)
openclaw/

.cache/
models/**/.cache/
models/*
!models/.gitkeep

.DS_Store
