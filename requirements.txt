# --- Web backend ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0,<3.0.0
requests>=2.31.0

# --- ML + HF stack ---
torch>=2.4.0
# Qwen3VLForConditionalGeneration requires transformers 4.52+
# (model config: transformers_version 4.57.0.dev0)
transformers>=4.52.0,<5.0.0
safetensors>=0.4.3
sentencepiece>=0.2.0
huggingface_hub>=0.22.0
accelerate>=0.33.0
Pillow>=10.0.0

# --- Sentence transformers ---
sentence-transformers>=3.0.0

# LeafHub SDK is an optional integration — install separately via the LeafHub
# one-liner (https://github.com/Rebas9512/Leafhub) if you want encrypted vault
# support.  Trileaf gracefully falls back to .env storage when it is absent.
